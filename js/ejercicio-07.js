const parImpar = prompt(`Ingrese un numero para verificar si es Par o Impar :`)

if ((parImpar % 2) == 0 ) {
    console.log(`El numero ${parImpar} es Par.`);
    
} else {
    console.log(`El numero ${parImpar} es Impar`);
    
}